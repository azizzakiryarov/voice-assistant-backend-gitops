apiVersion: v1
kind: Service
metadata:
  name: whisper
  namespace: voice-assistant
spec:
  selector:
    app: whisper
  ports:
    - name: http
      port: 9000
      targetPort: 9000
  type: ClusterIP